import 'package:flutter/material.dart';
import 'package:team1_e1/features/launches/data/models/launches_response.dart';

class LaunchesDetailsScreen extends StatelessWidget {
  LaunchesResponse launchesResponse;
  LaunchesDetailsScreen({super.key, required this.launchesResponse});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
